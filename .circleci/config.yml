version: 2.1

jobs:
  build:
    docker:
      - image: circleci/php:7.4-cli
    steps:
      - checkout
      - run: php -v
      - run: echo $DATABASE_URL
    environment:
      DATABASE_URL: $MY_Skrip

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build:
          context: my-org/my-context
